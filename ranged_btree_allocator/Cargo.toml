[package]
name = "ranged_btree_allocator"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
kernel_api = { version = "1.1.0", path = "../kernel_api" }
# ranged_btree = { path = "../ranged_btree" }
log = "0.4.21"

[dev-dependencies]
# kernel_api = { path = "../kernel_api", features = ["use_std"] }

[features]
kasan = []

[lints]
workspace = true
